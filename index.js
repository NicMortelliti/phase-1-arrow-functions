// Function expression called divide.
const divide = function() {
  // Divides 2000 by 100.
  // Expect divide (function) to equal 20.
  return 2000 / 100;
}

// Arrow function called square.
// Takes one parameter, multiplies it times itself.
const square = x => x * x;

// Arrow function called add.
// Takes two params and adds them together.
const add = (x, y) => x +y;